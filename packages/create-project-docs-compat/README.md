# create-project-docs

Compatibility wrapper for `@tu-cis-courses/create-project-docs`.

This package remains private during the scoped-package beta. It will be
published once as `2.1.0` when the scoped CLI reaches `1.0.0`, then deprecated
in favor of the scoped package.
