openapi: 3.0.3
info:
  title: Project API Placeholder
  version: 0.0.0
  description: |-
    Replace this starter OpenAPI document with the HTTP API exposed by your project.
    If your project does not expose an HTTP API, replace the OpenAPI page in the docs
    with the appropriate public interface documentation for your system.
servers:
  - url: /
    description: Replace with your API base URL.
paths: {}
components:
  schemas: {}
