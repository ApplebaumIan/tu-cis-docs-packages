# Reference Documentation Site

Private Docusaurus application used to validate workspace packages.

```bash
yarn install
ORG_NAME=ApplebaumIan PROJECT_NAME=tu-cis-docs-packages yarn build
```

Student projects should use `@tu-cis-courses/create-project-docs` or the
generated `tu-cis-4398-docs-template` repository.
